bootstrap_root:
    path:     /
    defaults: { _controller: BootstrapBundle:Docs:index, slug: index }

bootstrap_page:
    path:     /{slug}
    defaults: { _controller: BootstrapBundle:Docs:index, slug: 1 }
